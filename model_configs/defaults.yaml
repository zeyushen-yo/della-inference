job_name: ???
output: ???
tensor_parallel_size: ???
quantization: None

# nodes
nodes: 1
ntasks_per_node: 1
time: "12:00:00"

# compute
cpus_per_gpu: 12
gres: gpu:2
mem_per_gpu: 16G
constraint: gpu80

azure_user: ${azure_user} # you'll know if this is applicable. if you have to ask, it's not.

# additional commands
mail_type: fail
mail_user: veniamin@princeton.edu  # TODO: decide if this is the right email? maybe we get a communal email.